# QRD CLI

This crate provides the Phase 1 command-line scaffold for inspect, verify, convert, and keygen workflows.

## Current State

- Commands are present as placeholders.
- The CLI delegates to the Rust core where possible.
- This will be expanded with proper subcommand parsing and richer output in later work.
